version: 2.1
executors:
  my-custom-executor:
    docker:
      - image: cimg/ruby:2.7.3-browsers

jobs:
  bundle:

    executor: my-custom-executor
    steps:
      - checkout
      - run: |
          # echo Hello, World!

workflows:
  my-custom-workflow:
    jobs:
      - bundle